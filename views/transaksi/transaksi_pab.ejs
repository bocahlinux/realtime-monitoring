<!DOCTYPE html>
<html lang="id">
  <head>
    <%- include('../partials/head', { title: 'Input Transaksi PAB' }) %>
  </head>

  <body class="bg-gray-50 dark:bg-govdark min-h-screen text-slate-800 dark:text-slate-100">
    <!-- NAVBAR -->
    <%- include('../partials/navbar', { title: title }) %>

    <!-- WRAPPER (SIDEBAR + CONTENT) -->
    <div class="flex min-h-screen">
      <!-- SIDEBAR -->
      <%- include('../partials/sidebar') %>

      <!-- CONTENT -->
      <main class="flex-1  mt-[60px] w-full layout-container-separo px-4 sm:px-6 lg:px-8 py-4 sm:py-6 space-y-6">
        <div id="form-container"></div>
        <div id="table-container" class="mt-6"></div>
        <div id="modal-container"></div>
      </main>
    </div>

    <script src="https://unpkg.com/@supabase/supabase-js@2"></script>
    <script src="/js/pap_pab_manager.js"></script>
    <script>
      document.addEventListener("DOMContentLoaded", () => initPapPab('pab'));
    </script>
  </body>
</html>
